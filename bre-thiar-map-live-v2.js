'use strict';
const BUILD='hypnobius-map-v2.2';
const $=id=>document.getElementById(id),canvas=$('g'),ctx=canvas.getContext('2d');
ctx.imageSmoothingEnabled=false;
let VW=innerWidth,VH=innerHeight,DPR=1,run=false,ready=false,last=0,map=null,manifest=null;
let worldAtlases={},characterAtlases={},held=new Set(),toastTimer=0;
let player={x:690,y:540,f:'down',cls:'wayfarer',gender:'male',hp:100};
let collisions=[],triggers=[],worldObjects=[],party=[];
const classLabels={wayfarer:'Wayfarer',iron_warden:'Iron Warden',trail_ranger:'Trail Ranger',runekeeper:'Runekeeper'};
function asset(p){return p+'?v='+BUILD}
async function text(p){const r=await fetch(asset(p),{cache:'no-store'});if(!r.ok)throw new Error(p+' '+r.status);return r.text()}
async function json(p){return JSON.parse(await text(p))}
function imgURL(url){return new Promise((res,rej)=>{const i=new Image();i.onload=()=>res(i);i.onerror=()=>rej(new Error('Image decode failed'));i.src=url})}
async function cachedImage(spec){const paths=spec.parts||[spec.cache];let b64='';for(const p of paths)b64+=(await text(p)).replace(/\s/g,'');const bin=atob(b64),u=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)u[i]=bin.charCodeAt(i);const url=URL.createObjectURL(new Blob([u],{type:spec.mime||'image/png'}));try{return await imgURL(url)}finally{setTimeout(()=>URL.revokeObjectURL(url),5000)}}
function resize(){VW=innerWidth;VH=innerHeight;DPR=Math.min(3,devicePixelRatio||1);canvas.width=Math.round(VW*DPR);canvas.height=Math.round(VH*DPR);ctx.setTransform(DPR,0,0,DPR,0,0);ctx.imageSmoothingEnabled=false}addEventListener('resize',resize,{passive:true});resize();
async function load(){try{$('status').textContent='Loading Bré Thiar village map…';manifest=await json('bre-thiar-assets-v2.json');map=await json(manifest.map);for(const[name,s]of Object.entries(manifest.worldAtlases))worldAtlases[name]=await cachedImage(s);for(const[name,s]of Object.entries(manifest.characterAtlases))characterAtlases[name]=await cachedImage(s);parseMap();restore();setupClassMenu();ready=true;$('enter').disabled=false;$('status').textContent='Ready — Bré Thiar village hub'}catch(e){console.error(e);$('status').textContent='Load failed — '+e.message}}
function layer(n){return map.layers.find(l=>l.name===n)}
function parseMap(){const wf=(map.tilesets.find(t=>String(t.source||'').includes('world_objects'))||{firstgid:6}).firstgid;worldObjects=(layer('World Objects')?.objects||[]).map(o=>({...o,localId:o.gid-wf}));collisions=(layer('Collision')?.objects||[]).map(o=>({x:o.x,y:o.y,w:o.width,h:o.height,name:o.name}));triggers=layer('Spawns & Triggers')?.objects||[];const s=triggers.find(o=>o.name==='Player Spawn');if(s){player.x=s.x;player.y=s.y}party=[{name:'Brannoc',cls:'iron_warden',gender:'male',x:610,y:448,f:'down'},{name:'Eira',cls:'trail_ranger',gender:'female',x:835,y:445,f:'left'},{name:'Siofra',cls:'runekeeper',gender:'female',x:700,y:665,f:'up'},{name:'Maelíth',cls:'wayfarer',gender:'female',x:515,y:610,f:'right'}]}
function setupClassMenu(){const panel=document.querySelector('#sheet .panel');if(!panel||document.getElementById('appearance'))return;const box=document.createElement('div');box.id='appearance';box.innerHTML='<h3 style="color:#d7b85e;margin:14px 0 7px">Appearance QA</h3><div style="font:12px/1.4 system-ui;color:#cfc4a8;margin-bottom:6px">All eight current class/gender assets are live. Movement uses their real four directions; walk/combat cycles are not faked.</div>';for(const cls of Object.keys(classLabels)){const b=document.createElement('button');b.textContent=classLabels[cls];b.addEventListener('click',()=>{player.cls=cls;save();updateIdentity();toast(classLabels[cls]+' selected')});box.appendChild(b)}const gb=document.createElement('button');gb.textContent='Toggle Male / Female';gb.addEventListener('click',()=>{player.gender=player.gender==='male'?'female':'male';save();updateIdentity();toast(player.gender)});box.appendChild(gb);panel.insertBefore(box,$('reset'));updateIdentity()}
function updateIdentity(){const p=document.querySelector('.top .pill');if(p)p.textContent='Aedric · '+classLabels[player.cls]+' · '+player.gender}
function restore(){try{const q=JSON.parse(localStorage.getItem('bre-thiar-hypnobius-v2')||'null');if(q)player={...player,...q}}catch(_){}updateIdentity()}
function save(){localStorage.setItem('bre-thiar-hypnobius-v2',JSON.stringify({x:player.x,y:player.y,f:player.f,cls:player.cls,gender:player.gender,hp:player.hp}))}
function blocked(x,y){const hw=14,top=8,bot=5;if(x-hw<0||x+hw>map.width*map.tilewidth||y-top<0||y+bot>map.height*map.tileheight)return true;return collisions.some(r=>x+hw>r.x&&x-hw<r.x+r.w&&y+bot>r.y&&y-top<r.y+r.h)}
function move(dt){let dx=0,dy=0;if(held.has('left'))dx--;if(held.has('right'))dx++;if(held.has('up'))dy--;if(held.has('down'))dy++;if(!dx&&!dy)return;if(dx&&dy){dx*=.7071;dy*=.7071}const sp=126*dt,nx=player.x+dx*sp,ny=player.y+dy*sp;if(Math.abs(dx)>Math.abs(dy))player.f=dx<0?'left':'right';else player.f=dy<0?'up':'down';if(!blocked(nx,player.y))player.x=nx;if(!blocked(player.x,ny))player.y=ny;save();checkTriggers()}
function prop(o,k){const p=o.properties?.find(x=>x.name===k);return p?.value}
function checkTriggers(){for(const t of triggers){if(t.type!=='exit')continue;if(Math.hypot(player.x-t.x,player.y-t.y)<45){toast((prop(t,'destination')||t.name)+' — connected zone retained in legacy QA');break}}}
function zoom(){return VH>VW?1.22:Math.max(.92,Math.min(1.08,VH/720))}
function camera(){const z=zoom(),mw=map.width*map.tilewidth,mh=map.height*map.tileheight,w=VW/z,h=VH/z;return{z,w,h,x:Math.max(0,Math.min(mw-w,player.x-w/2)),y:Math.max(0,Math.min(mh-h,player.y-h/2))}}
function screen(x,y,c){return[(x-c.x)*c.z,(y-c.y)*c.z]}
function shadow(x,y,w,c,a=.24){const[sx,sy]=screen(x,y,c);ctx.save();ctx.fillStyle=`rgba(0,0,0,${a})`;ctx.beginPath();ctx.ellipse(sx,sy,Math.max(10,w*.19)*c.z,Math.max(4,w*.035)*c.z,0,0,Math.PI*2);ctx.fill();ctx.restore()}
function drawTerrain(c){const ts=map.tilewidth,fc=Math.max(0,Math.floor(c.x/ts)),lc=Math.min(map.width-1,Math.ceil((c.x+c.w)/ts)),fr=Math.max(0,Math.floor(c.y/ts)),lr=Math.min(map.height-1,Math.ceil((c.y+c.h)/ts));const im=worldAtlases[manifest.terrain.atlas],[ax,ay]=manifest.terrain.rect,sw=manifest.terrain.sourceTileWidth,sh=manifest.terrain.sourceTileHeight;for(const lname of ['Ground','Swamp','Roads']){const L=layer(lname);if(!L)continue;for(let ty=fr;ty<=lr;ty++)for(let tx=fc;tx<=lc;tx++){const gid=L.data[ty*map.width+tx];if(!gid)continue;const id=gid-1,sx=ax+(id%manifest.terrain.columns)*sw,sy=ay+Math.floor(id/manifest.terrain.columns)*sh,[dx,dy]=screen(tx*ts,ty*ts,c);ctx.drawImage(im,sx,sy,sw,sh,Math.floor(dx),Math.floor(dy),Math.ceil(ts*c.z)+1,Math.ceil(ts*c.z)+1)}}}
function drawWorld(o,c){const m=manifest.worldTiles[String(o.localId)];if(!m)return;const a=manifest.worldAtlases[m.atlas],im=worldAtlases[m.atlas],r=a.rects[m.key],w=o.width,h=o.height,[x,y]=screen(o.x-w/2,o.y-h,c);shadow(o.x,o.y,w,c,.18);ctx.drawImage(im,r[0],r[1],r[2],r[3],Math.round(x),Math.round(y),Math.round(w*c.z),Math.round(h*c.z))}
function drawCharacter(e,c,controlled=false){const s=manifest.characters[e.cls]?.[e.gender];if(!s)return;const im=characterAtlases[s.atlas],r=s.rect,row=manifest.directionRows[e.f||'down'],cw=128,ch=160,[x,y]=screen(e.x-cw/2,e.y-156,c);shadow(e.x,e.y,64,c,.32);ctx.drawImage(im,r[0],r[1]+row*ch,cw,ch,Math.round(x),Math.round(y),Math.round(cw*c.z),Math.round(ch*c.z));if(!controlled){const[sx,sy]=screen(e.x,e.y-172,c);ctx.font=`${Math.max(10,12*c.z)}px Georgia`;ctx.textAlign='center';ctx.fillStyle='#f2e7c9';ctx.strokeStyle='#08100a';ctx.lineWidth=3;ctx.strokeText(e.name,sx,sy);ctx.fillText(e.name,sx,sy)}}
function render(){if(!ready)return;const c=camera();ctx.setTransform(DPR,0,0,DPR,0,0);ctx.clearRect(0,0,VW,VH);ctx.fillStyle='#071009';ctx.fillRect(0,0,VW,VH);ctx.imageSmoothingEnabled=false;drawTerrain(c);const items=[];for(const o of worldObjects)items.push({y:o.y,k:'o',v:o});for(const p of party)items.push({y:p.y,k:'c',v:p});items.push({y:player.y,k:'p',v:player});items.sort((a,b)=>a.y-b.y);for(const it of items)it.k==='o'?drawWorld(it.v,c):drawCharacter(it.v,c,it.k==='p')}
function frame(t){if(!run)return;const dt=Math.min(.034,(t-last)/1000||0);last=t;move(dt);render();requestAnimationFrame(frame)}
function nearestTalkable(){const c=[...party.map(n=>({name:n.name,x:n.x,y:n.y,text:n.cls==='iron_warden'?'The east road has been quiet. Too quiet.':n.cls==='trail_ranger'?'The Rowanwood trail begins beyond the village roads.':n.cls==='runekeeper'?'The old altar in the swamp is awake again.':'Every road through Bré Thiar carries a story.'})),...triggers.filter(t=>t.type==='poi').map(t=>({name:t.name,x:t.x,y:t.y,text:t.name==='Swamp Shrine Trigger'?'The stones hum beneath the black water.':'Bré Thiar gathers around these crossroads.'}))];let b=null,d=1e9;for(const n of c){const q=Math.hypot(player.x-n.x,player.y-n.y);if(q<d){d=q;b=n}}return d<165?b:null}
function talk(){const n=nearestTalkable();if(!n)return toast('No one close enough to talk to.');$('dn').textContent=n.name;$('dt').textContent=n.text;$('dialog').classList.remove('hide')}
function attack(){toast(classLabels[player.cls]+' combat animation set is not authored yet.')}
function toast(s){const e=$('toast');e.textContent=s;e.classList.remove('hide');clearTimeout(toastTimer);toastTimer=setTimeout(()=>e.classList.add('hide'),1500)}
function bindDir(e){const d=e.dataset.d,on=x=>{x.preventDefault();held.add(d)},off=x=>{x.preventDefault();held.delete(d)};e.addEventListener('pointerdown',on);e.addEventListener('pointerup',off);e.addEventListener('pointercancel',off);e.addEventListener('pointerleave',off)}document.querySelectorAll('[data-d]').forEach(bindDir);
addEventListener('keydown',e=>{const k={ArrowUp:'up',KeyW:'up',ArrowDown:'down',KeyS:'down',ArrowLeft:'left',KeyA:'left',ArrowRight:'right',KeyD:'right'}[e.code];if(k){e.preventDefault();held.add(k)}});addEventListener('keyup',e=>{const k={ArrowUp:'up',KeyW:'up',ArrowDown:'down',KeyS:'down',ArrowLeft:'left',KeyA:'left',ArrowRight:'right',KeyD:'right'}[e.code];if(k)held.delete(k)});
$('talk').addEventListener('click',talk);$('atk').addEventListener('click',attack);$('menu').addEventListener('click',()=>$('sheet').classList.remove('hide'));$('close').addEventListener('click',()=>$('sheet').classList.add('hide'));$('dc').addEventListener('click',()=>$('dialog').classList.add('hide'));$('oldworld')?.addEventListener('click',()=>location.href='legacy-world.html');$('reset').addEventListener('click',()=>{const s=triggers.find(o=>o.name==='Player Spawn');player.x=s?.x||690;player.y=s?.y||540;player.f='down';save();$('sheet').classList.add('hide');toast('Position reset')});$('enter').addEventListener('click',()=>{if(!ready)return;$('splash').classList.add('hide');run=true;last=performance.now();requestAnimationFrame(frame)});load();
